# =============================================================================
# docker-compose.override.yml — Development overrides
# =============================================================================
# Docker Compose loads this file automatically alongside docker-compose.yml.
# Exposes microservice and infrastructure ports to the host for local development.
# In production, this file should NOT be used (e.g., using `docker compose -f docker-compose.yml up -d`).
#
# Swagger UIs (only available in development):
#   Users service        → http://localhost:8081/swagger-ui.html
#   Finances service     → http://localhost:8082/swagger-ui.html
#   Cards service        → http://localhost:8083/swagger-ui.html
#   Notifications svc    → http://localhost:8084/swagger-ui.html
#   Upload service       → http://localhost:8085/swagger-ui.html
#
# Production: docker compose -f docker-compose.yml --profile app up -d
# =============================================================================

services:
  # ─── Infrastructure ───────────────────────────────────────────────────────────
  traefik:
    ports:
      - "8888:8080" # Traefik dashboard

  postgres:
    ports:
      - "5432:5432"

  kafka:
    ports:
      - "9093:9093"

  minio:
    ports:
      - "9000:9000"
      - "9001:9001"

  # ─── Application services ─────────────────────────────────────────────────────
  gateway:
    ports:
      - "8080:8080"

  frontend:
    ports:
      - "3000:3000"

  service-users:
    # ports:
    #   - "8081:8081"

  service-finances:
    # ports:
    #   - "8082:8082"

  service-banks:
    # ports:
    #   - "8083:8083"

  service-notifications:
    # ports:
    #   - "8084:8084"

  service-upload:
    # ports:
    #   - "8085:8085"

  service-investments:
    # ports:
    #   - "8086:8086"
